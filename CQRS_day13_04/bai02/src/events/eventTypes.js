/**
 * Centralised event-type constants so strings aren't scattered everywhere.
 */
const EventTypes = {
  ORDER_CREATED: "ORDER_CREATED",
  ORDER_CANCELLED: "ORDER_CANCELLED"
};

module.exports = { EventTypes };
